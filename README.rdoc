== Configurar a APP

- Configure o `config/database.yml` com seu usuário e senha
- Exporte as variáveis GITHUB_KEY e GITHUB_SECRET
  Ex.: export GITHUB_KEY='SUA_CHAVE'
- Para startar `rake db:create db:migrate`, `bundle install` e depois `rails s`.
- Para testar `rspec spec`
